# Authentication boundary

This feature owns future identity forms, session queries, authorization policies, and API adapters. Session 1 deliberately leaves it unimplemented. The preserved login form routes to the shell through a temporary mock seam.
