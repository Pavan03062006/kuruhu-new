# Frontend package location

The existing Next.js application remains at the repository root to preserve every uploaded path and asset. This directory marks the frontend bounded context without moving or renaming the supplied project. A future workspace migration can move it here through a dedicated, reviewed change.
